apiVersion: v1
kind: Service
metadata:
  name: simple-mcp-service
  namespace: default
  labels:
    app: simple-mcp
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: simple-mcp
  sessionAffinity: None
